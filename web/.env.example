# 前端调用的 KOXPilot 服务地址（构建期注入；缺省回落到同源）
#
# 线上这一份用的就是下面这个值（部署记录见 ../api/DEPLOYED_URL.txt）：
VITE_API_BASE=$SERVICE_BASE
#
# 本地开发想打自己起的服务，就改成 make api-serve 的地址：
# VITE_API_BASE=http://127.0.0.1:8399
#
# 留空 = 回落到同源。同源下没有 /api 时，前端 800ms 内判定服务不可达，
# 自动切到浏览器引擎完成全流程（功能不缺项），界面会标明当前计算源。
