# 前端调用的 KOXPilot 服务地址（构建期注入；缺省回落到同源）
#
# 填一个自己能访问到的服务地址即可（部署记录与可用端点见 ../api/DEPLOYED_URL.txt）。
#
# 本地起的服务：
# VITE_API_BASE=http://127.0.0.1:8000
#
# 公网部署（Hugging Face Spaces，见 ../deploy/hf-space/README.md）：
# VITE_API_BASE=https://<你的-space-域名>
#
# 留空 = 回落到同源。同源下没有 /api 时，前端 800ms 内判定服务不可达，
# 自动切到浏览器引擎完成全流程（功能不缺项），界面会标明当前计算源。
VITE_API_BASE=
