# KOXPilot HTTP 服务的环境变量。**一个都不配也能正常跑**——
# 契约 §0.4 规定 LLM 是可选增强，数据与阈值都来自仓库里的真产物。

# ---------------------------------------------------------------------------
# 访问控制
# ---------------------------------------------------------------------------
# 默认公开可访问（契约 §0.3：校招评审是外部人，拿不到内部 JWT）。
# 需要收起来时置 1，脚手架那套 JWT 鉴权会原样生效（实现保留在 main.py，没有删）。
# REQUIRE_JWT=1

# 只有 REQUIRE_JWT=1 时才会用到下面三个：
# JWT_SERVER=cloud-boe-sandbox.byted.org
# JWT_USERINFO_URL=https://cloud-boe-sandbox.byted.org/auth/api/v1/userinfo
# JWT_USERINFO_TIMEOUT_SECONDS=5

# ---------------------------------------------------------------------------
# A1 brief 解析的模型凭据（可选）
# ---------------------------------------------------------------------------
# 配了就真调模型解析 brief，响应里 parse_path="llm"；
# 不配（或调用失败/超时）就走确定性规则解析，parse_path="rule"，接口照常返回 200。
# 取值口径与仓库根的 .env.example 完全一致，服务层没有另立一套（三种 provider 都支持）。
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=
# ARK_API_KEY=
# ARK_BASE_URL=
# ARK_MODEL=
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_DEPLOYMENT=

# 单次 brief 解析的超时（秒）。构建期批量跑可以等很久，在线接口不行：
# 有人在等页面出结果，所以默认 20 秒就降级到规则解析。
# KOXPILOT_LLM_TIMEOUT=20

# 明确禁用模型调用（比删 key 更清楚：health 里会如实写"已显式关闭"）。
# KOXPILOT_DISABLE_LLM=1

# ---------------------------------------------------------------------------
# 冷启动
# ---------------------------------------------------------------------------
# 数据集约 6.5MB JSON，进程起来后在后台线程加载；加载完成前 /api/health 返回
# status="warming"（契约 §3）。业务接口最多等这么久（秒）再放弃并如实报错。
# KOXPILOT_WARMUP_TIMEOUT=60
